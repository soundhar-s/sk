c="hello"
def anti_vowel():
    newstr = c
    vowels = ('a', 'e', 'i', 'o', 'u')
    for x in c.lower():
        if x in vowels:
            newstr = newstr.replace(x,"")
            print(newstr[::-1])
            print(newstr)
    return newstr
anti_vowel()
